import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    // output: 'export', // ou 'standalone' selon vos besoins
    // trailingSlash: true,
    // images: {
    //   unoptimized: true,
    // },
    // Désactiver le strict mode si nécessaire
    // reactStrictMode: false,
};

export default nextConfig;
